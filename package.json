{
  "name": "login",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "yarn clean && yarn vite build --base=./ && touch dist/.nojekyll",
    "preview": "vite preview",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.24",
    "@types/react-dom": "^18.0.8",
    "@vitejs/plugin-react": "^2.2.0",
    "node-sass": "^8.0.0",
    "rimraf": "^3.0.2",
    "sass": "^1.56.1",
    "vite": "^3.2.3"
  }
}
